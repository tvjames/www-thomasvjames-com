---
title: "Latest Posts"
layout: page
---

# Latest Posts

<% for post in @getCollection("posts").first(3): %>
  <%- @partial('post', {content: post.get('contentRenderedWithoutLayouts') or "**testing**", data: post.toJSON()}) %>
<% end %>
